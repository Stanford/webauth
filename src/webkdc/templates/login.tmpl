<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">
<!--
This is the template for the Stanford WebAuth v3 login page. This page
is called from login.fcgi script on the weblogin server. It displays a
form asking the user for username and password. Variables used:
username               : the username
password               : password
err_username           : no username error
err_password           : no password error
err_cookie             : err_cookie
RT                     : Response Token
ST                     : Service Token
LC                     : Login Cancel Token
CancelURL              : CancelURL
-->
<html>
  <head>
    <http-equiv="pragma" content="no-cache">
    <title>Webauth Login</title>
    <meta http-equiv="content-type" content="text/html; charset=iso-8859-1">
  </head>

  <body>
    <h1 align="center">Webauth Login</h1>
    <div class="error" align="center">
      <p><strong>
        <TMPL_IF NAME=err_missinginput>
          <!-- Error: Missing Username and Password. -->
          Error: Enter both your username and its password.
        <TMPL_ELSE>
          <TMPL_IF NAME=err_username>
            <!-- Error: no username submitted. -->
            Error: Enter your username.
          </TMPL_IF>     
          <TMPL_IF NAME=err_password>
            <!-- Error: no password submitted. -->
            Error: Enter your password.
          </TMPL_IF>
        </TMPL_IF>

        <TMPL_IF NAME=login_failed>
          <!-- Error: login failed. -->
          Error: You entered an incorrect username or password (or both).
        </TMPL_IF>

        <TMPL_IF NAME=err_cookies>
          <!-- Error: cookies not enabled. -->
          Error: You must enable cookies on your web browser.
        </TMPL_IF> 

      </strong></p>
      <p>Click <a href="/help.html">help</a> for assistance.</p>
    </div>

    <div class="loginbox" align="center">
      <table border="1" width="70%">
        <tr>
          <td><img alt="Login" src="/images/login.png"
                   width="160" height="160"/></td>
          <td>
            <div class="loginform">
              <form method="post" autocomplete="OFF"
                    enctype="application/x-www-form-urlencoded">
                <input type="hidden" name="RT" value="<TMPL_VAR NAME=RT>">
                <input type="hidden" name="ST" value="<TMPL_VAR NAME=ST>">
                <input type="hidden" name="LC" value="<TMPL_VAR NAME=LC>">
                <table>
                  <tr>
                    <td><strong>Username:</strong></td>
                    <td><input name="username" type="text"
                               value="<TMPL_VAR NAME=username>"
                               size="10" maxlength="8"><br/></td>
                  </tr>
                  <tr>
                    <td><strong>Password:</strong></td>
                    <td><input name="password" type="password" size="18"></td>
                  </tr>
                  <tr>
                    <td colspan="2" align="right">
                      <input type="submit" name="Submit" value="Login">
                    </td>
                  </tr>
                </table>
              </form>
            </div>
            <div class="loginlinks">
              <a href="/help.html">help</a>
              <TMPL_IF NAME=wa_cancel_url>
                <!-- The Webauth Server has a cancel url.-->
                <a href="<TMPL_VAR NAME=cancel_url>">cancel</a>
              </TMPL_IF>
            </div>
          </td>
        </tr>
      </table>
    </div>

    <div class="trailer">
      <h2>Caution:</h2>

      <p>Never enter your Webauth username and password on a web page
      unless the page is a page directly served by the Webauth login
      server.</p>
    </div>
  </body>
</html>
